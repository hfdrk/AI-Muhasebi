# Infrastructure as Code

This directory contains Infrastructure as Code (IaC) definitions for deploying the AI Muhasebi platform.

## Structure

- `terraform/`: Terraform modules and configurations
  - `modules/`: Reusable Terraform modules (database, redis, api, worker)
  - `environments/`: Environment-specific configurations (dev, staging, prod)

## Usage

Terraform configurations will be added in future tasks.

